// Define la URL base de tu backend (FastAPI)
export const BASE_URL = "https://127.0.0.1:8000";

// Define los endpoints específicos login
export const ENDPOINTS = {
    createGranja: `${BASE_URL}/api/granjas`,
};
